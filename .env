# Environment Variables

# LLM Configuration
# Options: "openai" or "ollama"
LLM_PROVIDER=ollama

# OpenAI Configuration (only needed if LLM_PROVIDER=openai)
OPENAI_API_KEY=

# Ollama Configuration (only needed if LLM_PROVIDER=ollama)
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=deepseek-r1:8b

# Offline Mode - Prevents HuggingFace from trying to connect to the internet
# Set to 1 to enable offline mode (models must be pre-downloaded)
HF_HUB_OFFLINE=1
TRANSFORMERS_OFFLINE=1

# Optional: Other services
# PINECONE_API_KEY=your_pinecone_api_key_here
# PINECONE_ENV=us-west1-gcp
# LANGCHAIN_API_KEY=your_langchain_api_key_here